<nav class="navbar navbar-expand-md navbar-light bg-light">
    <%= link_to "EstimationStation", root_path, class:"navbar-brand" %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav mr-auto">
        <%= link_to "All Rooms", rooms_path, class:"nav-item nav-link active"%>
        <!-- TODO: While hiding the link "works", authentication on logged in should be added to links -->
        <% if current_user %>
          <%= link_to "Create a Room", new_room_path, class:"nav-item nav-link active"%>
        <% end %>
      </div>
      <div class="navbar-nav ml-auto">
        <% if current_user %>
          <span class="navbar-text mr-2">Welcome, <%= current_user.email %>!</span>
          <%= link_to "Logout", destroy_user_session_path, method: :delete, class:"btn btn-outline-success btn-large"%>
        <% else %>
          <%= link_to "Login", new_user_session_path, class:"btn btn-outline-success btn-large"%>
        <% end %>
      </div>
    </div>
</nav>